import { injectable } from "inversify";

@injectable()
export class AuthService {
  findAll() {
    //
  }

  create(post: any) {
    //
  }

  findById(id: number) {
    //
  }

  update(id: number, post: any) {
    //
  }

  delete(id: number) {
    //
  }
}